name: cyber_ai
description: Cyber-Ai - Android app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter

  # External packages
  intl: ^0.20.2
  flutter_local_notifications: ^19.4.1
  network_info_plus: ^6.1.4
  permission_handler: ^11.3.1
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
